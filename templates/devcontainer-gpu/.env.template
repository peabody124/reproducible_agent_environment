# Environment variables for GPU devcontainer
# Copy this to .env in your project root (not .devcontainer/)
# IMPORTANT: .env should be in .gitignore - never commit secrets!

# Example: Hugging Face token for model downloads
# HUGGINGFACE_TOKEN=hf_your_token_here

# Example: Weights & Biases API key for experiment tracking
# WANDB_API_KEY=your_wandb_key_here

# GPU selection — defaults to no GPU so containers don't grab GPUs unexpectedly.
# Set CUDA_DEVICE_ORDER=PCI_BUS_ID so device IDs match nvidia-smi output.
# Change CUDA_VISIBLE_DEVICES to a GPU index (e.g. 0, 1) when you're ready to use one.
CUDA_DEVICE_ORDER=PCI_BUS_ID
CUDA_VISIBLE_DEVICES=-1
